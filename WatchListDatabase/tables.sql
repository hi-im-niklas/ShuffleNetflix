CREATE TABLE tUser
(
	UserID integer NOT NULL AUTO_INCREMENT,
    UserName varchar(40),
    Passwort varchar(40),
    Vorname varchar(40),
    Nachname varchar(80),
    PRIMARY KEY (UserID)
);

CREATE TABLE tSerie
    
    